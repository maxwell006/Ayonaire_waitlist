# Ayonaire_waitlist

This repository contains the Ayonaire waitlist app.

- Do not commit `.env` — it's excluded in `.gitignore`.

## Local setup

1. Install dependencies: `npm install`
2. Start server: `node server.js` (or `npm start` if configured)

## Next steps

Create a GitHub repo named `Ayonaire_waitlist` (or provide the remote URL) and then add it as a remote and push.
